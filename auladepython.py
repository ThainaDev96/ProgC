
# Online Python - IDE, Editor, Compiler, Interpreter

def sum(a, b):#def define as variáveis 
    return (a + b)

a = int(input('Enter 1st number: '))
b = int(input('Enter 2nd number: '))

print(f'Sum of {a} and {b} is {sum(a, b)}')
lista = [1,2,3,4,5]
#range tranforma uma lista em uma sequencia de numeros 
print (lista)
for i in range(len(lista)):
    print(f"posicao[{i}]) : {lista[i]}")
    #chaves para representa um parametro ou valor do F de função
print(f"Obrigado por usar nossa aplicação")

